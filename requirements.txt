# Adaptive Document Intelligence System - Dependencies
# Python 3.11+ compatible

# Core OCR - Tesseract (REQUIRED)
pytesseract>=0.3.10

# Image Processing (REQUIRED)
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Testing (REQUIRED)
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: Progress bars
tqdm>=4.66.0

# Optional: PaddleOCR (NOT required, only if user wants it)
# paddlepaddle>=2.5.0
# paddleocr>=2.7.0